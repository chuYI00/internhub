# 岗位表导入教程（30 秒版）

入口：**「🎯 岗位」页签最上面的「📥 导入我的岗位表」**。四条通道都在那一处，选一条走完就行。

| 通道 | 什么时候用 | 你要做什么 |
|---|---|---|
| **① 上传 CSV / Excel** | 手里已经有文件（采集助手导出的、Excel 另存的、卖家给的） | 选文件 → 下一步 |
| **② 复制粘贴** | 飞书/Excel 里能框选 Ctrl+C | 框选 → Ctrl+C → 粘进框 → 点解析 |
| **③ 抓包解码** | 飞书**多维表格**被禁复制、禁导出、禁打印（你买的那份就是） | F12 抓包 → 用 `解码飞书表格.py` 解成 CSV |
| **④ 截图 OCR** | 上面三条全被拦（兜底，100% 能成） | 通道还在做；现在把截图发给阿枢，我帮你转成 CSV 走通道① |

> 飞书文档被禁复制？按《飞书只读表格导出实操指南.md》操作 —— 浏览器扩展一键导出，
> 或 F12 抓包后用项目里的 `解码飞书表格.py` 解成 CSV。

---

## 通用流程（四条通道都一样）

```
选通道 → 解析出原始表格 → 预览前 8 行 → 确认字段映射 → 勾选"我已核对" → 确认入库
```

**字段映射是给人看的**：你的列名千奇百怪（"公司名称 / 单位 / 雇主"…），系统先猜一遍，
猜错的下拉框里改。**没用到的列不会丢**，全部并进「备注」。

标准字段只有 7 个：

| 标准字段 | 能认的列名（举例） |
|---|---|
| 岗位名称 * | 岗位、职位、岗位名称、招聘职位、招聘岗位 |
| 公司 | 公司、单位、公司名称、用人单位、企业、集团 |
| 城市 | 城市、工作城市、地区、地点、所在地 |
| 投递链接 | 链接、投递链接、官网链接、报名网址、投递入口、公告链接 |
| 来源 | 来源、渠道、平台、数据来源 |
| 报名截止 | 截止、截止时间、报名截止、投递截止、有效期 |
| 备注 | 备注、说明、注释 |

`*` 岗位名称必填 —— 没有岗位名的行会被跳过（不是丢，是没法入库）。

---

## 通道① 上传 CSV / Excel

1. 选文件：`.csv` / `.xlsx` / `.txt` 都行。
2. Excel 直接传 **.xlsx** 就行（工具内置了读取，**不用装 openpyxl**）；老版 `.xls` 请另存为 `.xlsx` 或 CSV。
3. 编码不用管：UTF-8 / UTF-8-BOM / GBK 都会自动试。

> 图示位置 ①：向导「① 上传 CSV / Excel」区域，虚线框中间一个「Drag and drop file here」，
> 下面一行小字 `Limit 200MB per file · CSV, XLSX, XLSM, TXT`。

---

## 通道② 复制粘贴（飞书多维表格能框选时最省事）

1. 在飞书多维表格 / Excel / WPS 里 **选中表格区域 → Ctrl+C**。
2. 粘进向导的框 → 点 **🔍 解析**。
3. **没复制表头也能用**：只框选了数据行时会自动按内容猜列（网址→链接、城市名→城市、
   日期→截止、含"公司/局/厂/工业…"→公司），**第一行不会被吃掉**。

更省事的办法：复制完直接双击项目里的 **`从剪贴板导入.bat`** —— 连打开网页都省了。

> 图示位置 ②：向导「② 复制粘贴」区域，一个大文本框，占位文字是
> `岗位名称 公司 工作城市 投递链接 / 数据分析工程师 云南机场集团 昆明 https://...`。

---

## 通道③ 抓包解码（多维表格被禁复制时，成功率最高）

飞书的"禁止复制/导出/打印"都是**前端网页层面**的限制，数据本身已经完整传到你的浏览器了 —— 所以能抓。

1. 浏览器打开那个飞书表，**等它完全加载**。
2. **F12** → **Network（网络）** → **F5 刷新**。
3. 点 **Size** 列**从大到小排序**，逐条点开 **Preview**，
   找到能看到岗位记录（公司名、岗位名）的那条。
4. 右键 → **Copy response**，整串存成 `抓包.txt`
   （看到一大串 **`H4sI` 开头**的 base64 也照样存，那正是我们要的）。
5. 在项目文件夹 `D:\罗广睿\intern_tool` 里跑：
   ```
   venv\Scripts\python.exe 解码飞书表格.py 抓包.txt
   ```
6. 会生成 `data\飞书岗位导出.csv` → 回向导点 **📂 读取 data/飞书岗位导出.csv** 即可
   （也可以把那个 CSV 用通道① 上传，效果一样）。

**⚠️ 大概率会遇到"分页"**：多维表格的接口一次只给一部分（常见 20 / 100 条）。
解码器会告诉你服务端自报多少条，比如 `接口自报总条数是 249，你这次只拿到 20 条`。
这时**再抓几次**（把表往下滚、让它继续加载，每次 Copy response 存一个 txt），然后一次传多个文件：

```
venv\Scripts\python.exe 解码飞书表格.py 抓包1.txt 抓包2.txt 抓包3.txt
venv\Scripts\python.exe 解码飞书表格.py 抓包目录\          # 目录里所有 txt/json 一起解
```

多个文件会**按 record_id 或内容自动合并去重**，重叠的不会重复入库。

> 小技巧：在 F12 的 Network 面板里筛 `records`（或 `bitable`），
> 列表里会有好几条响应；按 Size 降序，**逐条 Copy response** 各存一个 txt 最省事。

解码器全自动：认得出 gzip+base64、裸 JSON，飞书的嵌套富文本字段也会展平。
**纯本地运行、不联网、不上传任何数据。**

完整图文步骤（含浏览器扩展、控制台脚本等备选路线）见
**《飞书只读表格导出实操指南.md》** —— 向导里也内置了这份文档，展开就能看。

> 图示位置 ③：F12 Network 面板，Size 列降序，选中一条请求，右侧 Preview 里能看到
> `公司名称 / 岗位名称 / 工作城市` 这样的字段。

---

## 通道④ 截图 OCR（兜底，100% 能成）

屏幕上能看到的东西就一定能截下来。

1. **Win+Shift+S** 逐屏截图（**按列截**比整屏截识别率高）。
2. 通道还在做（RapidOCR 依赖较重）。现在把截图直接发给阿枢，我帮你识别成 CSV 再走通道①。

---

## 常见问题

| 现象 | 原因 / 怎么办 |
|---|---|
| 导入成功但 0 条 | 没识别出"岗位"列且第一列是空的 → 在映射里手动把岗位名那列选上 |
| **只进来 20 条，可表里有 249 条** | 接口是分页的 → 再抓几次，一次传多个文件（见通道③ 的"分页"说明），解码器会自动合并去重 |
| **岗位名全是 `2026/09/16` 这种日期** | 那份表第一列是「更新日期」、没有岗位列 → 已自动退回**用公司名当岗位名**；也可以在映射里手动把「岗位名称」指到公司名称那列 |
| 岗位名被认成了别的列 | 映射下拉里改「岗位名称」指向的列，看一眼下面的"转换后前 5 条"再确认 |
| 提示"没识别到表头" | 正常，系统按内容猜了列；**第一行也当数据导进来了**，不会丢 |
| 链接列有内容但不是网址 | 飞书「超链接」字段复制出来常是显示文字 → 回表格把该列改成 URL 类型再复制 |
| 导了两次 | 不会重复入库：按「岗位名+公司+城市+链接」去重，第二次只更新 |
| 中文乱码 | 极少见；把文件另存为 **CSV UTF-8（带 BOM）** 再传 |
