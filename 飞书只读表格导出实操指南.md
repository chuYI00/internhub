# 飞书只读表格导出实操指南（禁止复制 / 导出 / 打印都能解）

> 你的场景：淘宝买的飞书**多维表格**岗位汇总，只读、不能复制、不能导出、不能打印。
> 原理：飞书这些限制都是**前端网页层面**的（禁按钮、禁右键、禁 Ctrl+C），内容本身已经完整传到了你的浏览器里——所以有路可走。按下面顺序试，前面不行再往后。

---

## 路线 0（最省事，先试）：找卖家要

淘宝买的，直接找卖家要一份**可编辑副本或 Excel 文件**——这种岗位表卖家手里一定有源文件。一句话的事，能省掉下面所有步骤。

---

## 路线 1（首选）：浏览器扩展一键导出

**Cloud Document Converter**（开源免费，本地转换、内容不上传第三方服务器）

1. Chrome / Edge 扩展商店搜 **Cloud Document Converter**，安装。
2. 打开那个飞书文档，点扩展图标，按提示三步导出 **Markdown / PDF / Word**。
3. 导出的 PDF/Word → 回 InternHub 岗位页「📥 导入我的岗位表」上传。

注意：它对飞书"文档"支持最好；你买的如果是"多维表格"，看扩展页面说明是否覆盖，不覆盖就走路线 2。

## 路线 2（多维表格专用）：F12 抓包 + 解码器（成功率最高）

多维表格的数据在浏览器联网加载时是完整传过来的，抓下来解一下就行。**项目里已备好现成解码器 `解码飞书表格.py`（已自测通过）。**

步骤（照抄即可，不用懂原理）：

1. 电脑浏览器打开那个飞书多维表格，等它完全加载出来。
2. 按 **F12** 打开开发者工具 → 顶部切到 **Network（网络）** 标签。
3. 按 **F5 刷新页面**，等请求刷出来。
4. 点 Size（大小）列**从大到小排序**——最大的那几条就是表格数据。
5. 逐条点开看 **Preview（预览）**，找到能看到岗位记录（公司名、岗位名等）的那一条：
   - 如果 **Response / 预览**里能看到 JSON → 右键 → Copy response（复制响应），存成 `抓包.txt`。
   - 如果看到的是一大串 **`H4sI` 开头的字母数字**（base64）→ 整串复制，存成 `抓包.txt`。
   - 如果请求 URL 里带一长串参数，手动访问那个 URL，把返回的整串内容存下来。
6. 在项目文件夹 `D:\罗广睿\intern_tool` 里打开终端，跑：
   ```
   venv\Scripts\python.exe 解码飞书表格.py 抓包.txt
   ```
7. 成功的话会输出 `data\飞书岗位导出.csv`（含行数和预览）→ 回 InternHub「📥 导入我的岗位表」上传这个 CSV，字段映射确认后入库。

> 解码器全自动：认得出 gzip+base64、裸 JSON，飞书的嵌套字段（富文本）也能展平。没认出来会明确报错，把报错丢给阿枢看。

## 路线 3：控制台脚本解除复制限制

对飞书"文档"类有效；多维表格是画布渲染，可能无效，试 30 秒不行就跳过。

1. 打开文档 → **F12** → **Console（控制台）**。
2. 粘贴下面整段代码，回车（会提示 ✅）：

```javascript
(() => { const EVENTS = ['copy','cut','paste','contextmenu','selectstart','dragstart','keydown','keyup']; const STYLE_CSS = '*{user-select:text!important;-webkit-user-select:text!important}'; const install = (win) => { try { const doc = win.document; if (!doc.getElementById('__freecopy_style__')) { const s = doc.createElement('style'); s.id='__freecopy_style__'; s.textContent = STYLE_CSS; doc.head.appendChild(s); } const H = e => { if (e.type === 'keydown' || e.type === 'keyup') { const k = (e.key||'').toLowerCase(); const ctrlLike = e.ctrlKey || e.metaKey; if (!(ctrlLike && k === 'c')) return; } e.stopImmediatePropagation(); }; [win, doc, doc.documentElement, doc.body].forEach(t => { if (!t || !t.addEventListener) return; EVENTS.forEach(ev => t.addEventListener(ev, H, true)); }); } catch {} }; const harden = (win) => { install(win); try { win.document.querySelectorAll('iframe').forEach(f => { try { if (f.contentWindow && f.contentDocument) install(f.contentWindow); } catch {} }); } catch {} }; let t0 = performance.now(); (function loop(){ harden(window); if (performance.now() - t0 < 20000) requestAnimationFrame(loop); })(); console.log('OK 已放开复制（20 秒内有效）'); })();
```

3. 20 秒内回到文档，框选表格 → Ctrl+C → 回 InternHub 用"复制粘贴"通道导入。

## 路线 4（兜底，100% 能成）：截图 OCR

屏幕上能看到的东西就一定能截下来：

1. **Win+Shift+S** 逐屏截图（表格区域）。
2. 截图交给阿枢 / DeepSeek OCR 成表格，或等导入向导的 OCR 通道上线后直接在工具里传图。
3. 截图时**按列截**比整屏截识别率高。

---

## ⚠️ 安全提醒

- 网上有些"飞书导出工具"（如 app.jscs.top 之类）**要求你登录飞书授权**——第三方拿到你账号授权有风险。岗位表不敏感可以试，但**别用重要飞书账号去授权**，优先用上面本地转换的扩展和脚本。
- `解码飞书表格.py` 纯本地运行、不联网、不上传任何数据，放心用。

## 推荐顺序

**路线 0（找卖家）→ 路线 2（抓包解码，多维表格最对症）→ 路线 1（扩展）→ 路线 3 → 路线 4**
