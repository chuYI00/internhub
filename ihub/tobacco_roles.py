# -*- coding: utf-8 -*-
"""🍃 烟草岗位类型推荐：按「合适 / 轻松 / 钱多」三维打分排序。

为什么单独做这个模块
--------------------
烟草公告一发下来，岗位表里动辄几十个类别，名称还都挺唬人。你没法一条条去查
"这个累不累、钱多不多"。所以这里把烟草系统常见的岗位类别拆开，按你说的三个维度打分：

    合适 = 与「电子信息与自动化 / 物联网工程」的专业对口度（专业能否过审 + 面试有优势）
    轻松 = 日常强度（有无倒班、值班、跑外勤、季节性高强度、指标压力）
    钱多 = 新入职本科的收入水平（基本工资 + 绩效 + 津补贴，以云南为例）

分数 1~5，**越高越好**。权重可以自己调（页面上有滑条），排序会跟着变。

⚠️ 数据口径说明：分数基于公开信息与行业普遍情况整理，**不是官方数据**；
   各类别的叫法各省略有差异（有的省写「信息化类」，有的写「计算机类」），
   以当年公告的岗位表为准。收入只是区间参考，不作为承诺。
"""

DIMS = ["合适", "轻松", "钱多"]

# 每个岗位类别：fit=专业对口, light=强度/轻松度, pay=收入, yunnan=云南情况, trap=坑
ROLES = [
    {
        "name": "数字信息类 / 信息化技术岗",
        "alias": ["数字信息类", "信息化", "信息技术", "信息中心", "计算机类", "网络安全", "大数据"],
        "fit": 5, "light": 4, "pay": 4,
        "duty": "省局/州局机关、中烟信息中心的系统运维、数据管理、网络安全、业务系统开发对接",
        "yunnan": "**烟草系统里最适合你的岗**：省局有信息中心，各州市局也有信息化岗；昆明、大理都可能有",
        "trap": "岗位少、报名人数多，是「技术岗里最卷」的一类；但专业卡得严，反而不容易被跨专业挤掉",
        "fit_note": "「信息与通信工程 / 电子信息 / 计算机」专业目录直接对口，你的物联网工程可归入电子信息类",
    },
    {
        "name": "机电自动化类 / 设备技术管理岗",
        "alias": ["机电自动化类", "设备管理", "电气", "自动化", "仪表", "智能制造", "动力能源"],
        "fit": 5, "light": 3, "pay": 4,
        "duty": "卷烟厂智能产线的设备管理、技术改造、自动化系统维护、能源动力系统管理",
        "yunnan": "云南中烟招录**体量最大**的一类，昆明/玉溪/曲靖/红河等厂区都有；大理也有厂区配套",
        "trap": "有的岗位要轮流值班或参与设备抢修（不是纯办公室岗），面试会问「能不能接受倒班」",
        "fit_note": "自动化 / 电气 / 机电一体化专业目录对口，你和我专业几乎完全重合",
    },
    {
        "name": "质量检验 / 技术中心（检测、计量）",
        "alias": ["质量检验", "技术中心", "检测", "计量", "标准化", "烟草研究院", "质检站"],
        "fit": 4, "light": 4, "pay": 4,
        "duty": "卷烟/烟叶理化指标检测、仪器计量校准、技术标准与质量控制",
        "yunnan": "省局质检站、中烟技术中心、烟草研究院的岗位；昆明为主",
        "trap": "部分岗位要接触化学试剂或进入车间取样；招聘人数通常不多",
        "fit_note": "仪器仪表、电子测量与你的专业课有交叉，属于「擦边对口、好上岸」" '',
    },
    {
        "name": "综合管理类（人事 / 行政 / 文秘 / 党建）",
        "alias": ["综合管理类", "管理类", "行政", "文秘", "人力资源", "党建", "政工"],
        "fit": 2, "light": 4, "pay": 4,
        "duty": "机关科室的综合事务、材料写作、会务、人事",
        "yunnan": "各层级机关都有，岗位数量中等",
        "trap": "**专业不限或限制很松 → 报名人数爆炸**，是最难上岸的一类；且写材料是硬活",
        "fit_note": "你的专业不是优势项，纯拼笔试分数和面试表达",
    },
    {
        "name": "专卖管理 / 稽查执法",
        "alias": ["专卖管理", "稽查", "执法", "市场监管", "烟草专卖"],
        "fit": 2, "light": 2, "pay": 4,
        "duty": "市场监管、案件查处、走访零售户、打假打私",
        "yunnan": "各州市局、县局都有，招人不少",
        "trap": "**外勤多、要跑市场、有执法风险**，和你「不要一线」的定位冲突",
        "fit_note": "通常要求法律、公安类专业，或专业不限",
    },
    {
        "name": "卷烟营销 / 客户经理",
        "alias": ["营销", "客户经理", "销售", "访销", "市场经理"],
        "fit": 2, "light": 2, "pay": 4,
        "duty": "对接零售户、催订催销、跑市场、背销量指标",
        "yunnan": "各州市局（公司）都有，招录量不小",
        "trap": "**指标压力大**（有的地方与绩效强挂钩），要天天跑店，属于典型一线",
        "fit_note": "多为市场营销/经济类专业，你报就是跨专业，优势为零",
    },
    {
        "name": "烟叶生产 / 烟草农业技术",
        "alias": ["烟叶", "烟叶生产", "烟草农业", "生产技术", "烟站"],
        "fit": 3, "light": 1, "pay": 3,
        "duty": "烟叶种植技术指导、收购季驻站、烟农培训",
        "yunnan": "云南是烟叶大省，这一类招录体量很大",
        "trap": "**收购季驻村驻点、季节性高强度**（常年最累的一类之一），地点多在乡镇",
        "fit_note": "农学/植保为主；若岗位写「智慧农业/农业物联网」才和你有交集",
    },
    {
        "name": "物流 / 仓储 / 分拣",
        "alias": ["物流", "仓储", "分拣", "配送", "卷烟物流"],
        "fit": 2, "light": 3, "pay": 3,
        "duty": "卷烟仓储、分拣线管理、配送调度",
        "yunnan": "各州市物流中心",
        "trap": "分拣线可能要跟着班次走",
        "fit_note": "物流工程/管理类专业更对口",
    },
    {
        "name": "生产操作类（卷烟机操作 / 车间一线）",
        "alias": ["生产操作类", "生产操作", "操作岗", "车间", "一线", "卷烟机"],
        "fit": 2, "light": 1, "pay": 3,
        "duty": "卷烟机/包装机操作、车间流水线作业",
        "yunnan": "招录量很大，学历门槛相对低",
        "trap": "**三班倒、噪音粉尘环境**，你已明确不投一线 —— 这一条就是给「钱多轻松」兜底的对比项",
        "fit_note": "机电类专业可以报（这也是很多人被吸引进来的原因），但工作形态是一线",
    },
]


def score(role: dict, w_fit: float = 0.4, w_light: float = 0.3, w_pay: float = 0.3) -> float:
    """加权总分（10 分制，便于看）。权重不必归一化。"""
    total_w = (w_fit + w_light + w_pay) or 1.0
    s = (role["fit"] * w_fit + role["light"] * w_light + role["pay"] * w_pay) / total_w
    return round(s * 2, 1)          # 1~5 → 2~10 分


def ranked(w_fit: float = 0.4, w_light: float = 0.3, w_pay: float = 0.3) -> list[dict]:
    """按总分排序返回（附 score 字段）。"""
    out = []
    for r in ROLES:
        item = dict(r)
        item["score"] = score(r, w_fit, w_light, w_pay)
        out.append(item)
    return sorted(out, key=lambda x: -x["score"])


def by_alias(text: str) -> dict:
    """按公告/岗位名里的关键词猜是哪一类（找不到返回 None）。"""
    t = text or ""
    best, best_len = None, 0
    for r in ROLES:
        for a in r["alias"]:
            if a in t and len(a) > best_len:
                best, best_len = r, len(a)
    return best


def stars(n: int) -> str:
    return "★" * n + "☆" * (5 - n)


def summary_text(w_fit: float = 0.4, w_light: float = 0.3, w_pay: float = 0.3) -> str:
    """一句话结论（页面上直接用）。"""
    top = ranked(w_fit, w_light, w_pay)
    a, b, c = top[0], top[1], top[2]
    return (f'按你现在这套权重，**最该报 {a["name"]}**（{a["score"]} 分），'
            f'其次 {b["name"]}、{c["name"]}。'
            f'最不划算的是 {top[-1]["name"]}（{top[-1]["score"]} 分）。')


def as_text(w_fit: float = 0.4, w_light: float = 0.3, w_pay: float = 0.3) -> str:
    L = ["🍃 烟草岗位类型推荐（合适 / 轻松 / 钱多）", "=" * 60, ""]
    L.append(f"权重：合适 {w_fit:.2f}　轻松 {w_light:.2f}　钱多 {w_pay:.2f}")
    L.append(summary_text(w_fit, w_light, w_pay))
    L.append("")
    L.append(f'{"排名":<4}{"岗位类别":<28}{"总分":<6}{"合适":<6}{"轻松":<6}{"钱多":<6}')
    L.append("-" * 60)
    for i, r in enumerate(ranked(w_fit, w_light, w_pay), 1):
        L.append(f'{i:<5}{r["name"][:26]:<28}{r["score"]:<7}'
                 f'{stars(r["fit"]):<6}{stars(r["light"]):<6}{stars(r["pay"]):<6}')
    L.append("")
    for i, r in enumerate(ranked(w_fit, w_light, w_pay), 1):
        L.append(f'【{i}. {r["name"]}】总分 {r["score"]}　合适{stars(r["fit"])}　'
                 f'轻松{stars(r["light"])}　钱多{stars(r["pay"])}')
        L.append(f'  干什么：{r["duty"]}')
        L.append(f'  云南情况：{r["yunnan"]}')
        L.append(f'  坑：{r["trap"]}')
        L.append(f'  专业匹配：{r["fit_note"]}')
        L.append("")
    L.append("⚠️ 分数是按公开信息与行业普遍情况整理的**参考值，不是官方数据**；")
    L.append("   岗位类别各省叫法不同（信息化类/计算机类），以当年公告的岗位表为准。")
    L.append("   投递前务必核对公告岗位表的专业目录要求。")
    return "\n".join(L)
