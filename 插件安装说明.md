# 插件安装说明（网申助手 / 采集助手）

> 两个助手都是**浏览器用户脚本（.user.js）**，需要先装 **Tampermonkey（油猴）** 扩展，再把脚本装进去。
> 全程只在你本机、你自己点击时生效：**不登录、不提交、不抓接口**。

## 第一步：装 Tampermonkey（油猴）
**方法 A（推荐，国内可直连）：用 Edge 浏览器**
1. 打开 Edge → 右上角 `…` → **扩展** → **获取 Microsoft Edge 扩展**
2. 搜索 **Tampermonkey** → 点 **获取 / 添加扩展**
3. 装好后浏览器右上角会出现一个**黑色小图标**（拼图里点"显示在工具栏"）

**方法 B（Chrome）**
- 能访问 Chrome 应用商店：搜索 Tampermonkey → 添加至 Chrome
- 打不开商店：用方法 A 的 Edge，或从官网 <https://www.tampermonkey.net/> 下载安装

## 第二步：装两个脚本（两种方式，任选）

### 方式 1：拖拽安装（最快）
1. 打开文件夹：`D:\罗广睿\intern_tool`
2. 把 **`网申助手.user.js`** 直接**拖进浏览器窗口** → 弹出安装页 → 点 **安装**
3. 同样把 **`采集助手.user.js`** 拖进浏览器 → 点 **安装**
> 若拖拽没反应：Chrome/Edge 需先在 `扩展管理页` 打开 **开发者模式**；或者改用方式 2。

### 方式 2：粘贴安装（最稳）
1. 点浏览器右上角 **Tampermonkey 图标** → **添加新脚本**（Dashboard → ➕）
2. 打开 `网申助手.user.js`（记事本即可）→ **全选 → 复制**
3. 回到油猴编辑器：**删掉里面的模板内容** → **粘贴** → 按 **Ctrl + S** 保存
4. 重复同样步骤装 `采集助手.user.js`

## 第三步：验证是否装好
- 打开任意网页（例如 https://www.zhipin.com 或 https://www.iguopin.com ）
- 页面**右下角**应出现按钮：
  - **📝 填入我的资料**（网申助手）
  - **📥 采集本页岗位**（采集助手）
- 点油猴图标 → **管理面板**，能看到这两条脚本（状态为"已启用"）

## 怎么用

### 📝 网申助手 v3（按岗位方向填表，重点看这个）

**它长什么样**：任意网页右下角有个 **「📝 网申助手」** 小胶囊按钮 → 点开是一个**资料面板**。

**三步用法**
1. 打开某公司的**网申/在线申请页**（账号密码你自己登，脚本不碰登录）
2. 点右下角 **📝 网申助手** → 面板展开。**先在面板第一行选「岗位方向」**（9 套：万能通用 / 嵌入式 / IoT / AI 大模型 / 自动化 / 测试运维 / 民航国企 / 产品 / 内容运营）——选完，自我评价、技能特长、个人亮点会自动换成该方向的口径；带 `*` 的字段是你在这里改过的值，改完自动记在本机浏览器里
3. 选 **「填入空字段」**（推荐，不动已填的）或 **「覆盖全部」** → 填过的输入框会显示**绿色边框**（选下拉/单选的打勾）→ 你核对无误后**自己点提交**

**能填什么**（40 类字段，新增 学院 / 申请理由 / 婚姻状况 等）
姓名、性别、出生日期、政治面貌、民族、身份证号、手机、邮箱、微信、QQ、毕业院校、学院、专业、学历、入学时间、毕业时间、就读起止、GPA、专业排名、现居地、籍贯/生源地、通讯地址、邮编、外语水平、证书、奖学金荣誉、求职意向、期望城市、期望薪资、到岗时间、自我评价、项目/实习经历、技能特长、个人亮点、紧急联系人（姓名/电话/关系）、婚姻状况

**支持的控件**（这是 v1 做不到的）
| 控件 | 说明 |
| --- | --- |
| 文本框 / 多行文本 | 姓名、自我评价、项目经历等长文本 |
| **下拉框 select** | 性别、学历、籍贯、现居地；学历会自动把「本科」匹配到「**本科及以上**」，籍贯「云南大理」匹配到「**云南省**」 |
| **单选 radio** | 性别、政治面貌这类选项组（自动点中正确的那一项） |
| 日期 / 月份 | 自动转成 `2027-06-01` / `2027-06` 格式 |
| 数字框 | GPA 自动把 `3.47/4.0` 变成 `3.47` |
| 富文本 contenteditable | 有些系统用 div 当输入框，也能写进去 |
| **iframe 里的表单** | 很多网申系统（北森/Moka/Workday 等）把表单嵌在 iframe 里 —— 点一次会向所有子框架广播，一样能填 |

**为什么 v1 你"感觉用不了"**：v1 把"整个表单容器的文字"当成字段名，所以一个大 div 里的所有输入框都会被填成同一个值（等于乱填）。v2 改成只采信**控件数 ≤2 的小容器**、`<label for>`、`placeholder`、`aria-label`、`name/id`、表格上一格等线索，并且**取"命中关键词最长"的那个字段**，所以不会再把"备注"填成姓名。

**资料从哪来**
- 网站「👤 我的资料」页 → ③ 下面有个 **「④ 网申补充字段」** 折叠区，把政治面貌/籍贯/学院/期望城市等填上（**留空 = 自动跳过该字段**）
- 也可以在网页上的面板里**当场补**（存在浏览器 localStorage，标了 `*` 的就是你改过的）
- 改完资料后重新生成脚本并覆盖安装：网站「👤 我的资料」页点「生成/更新 网申助手.user.js」，或命令行：
  ```bat
  venv\Scripts\python.exe -m ihub.autofill
  ```

**装好后怎么确认它真的生效**：打开任意一个网申页 → 面板第 1 行状态会写"已填 N 个字段：姓名→罗广睿，手机号→135…"。如果是"已填 0 个"，说明该页字段名很特殊 —— **把那个页面的网址发我**，我加匹配规则。

### 🌐 全网平台入口（在「🌏 云南秋招」页签顶部）

打开网页 → **「🌏 云南秋招」** 页签，最上面就是：

1. **今天该干什么**：6 步，每步一个「打开」按钮，照着点就行
2. **4 类平台**（云南本地默认展开）：每个平台三个按钮 ——
   「🔗 打开官网」「🔍 搜云南应届岗」「💬 微信搜公告」
3. **搜索关键词**：专业词（嵌入式/物联网/自动化…）+ 云南行业词（烟草/电网/机场/地铁）
   —— **一个平台换几个词搜**，只搜一个词会漏掉一半岗位
4. **全部搜索直达链接**：一块代码框，右上角一键复制，能存手机

> 为什么不做"全自动抓"：实测 BOSS直聘、智联、前程无忧、猎聘、牛客、国聘网、24365、
> 高校人才网 **全是前端渲染 + 登录墙**，静态爬虫拿不到岗位（硬爬违反 robots 且随时失效）。
> 所以改用**搜索引擎站内直达**：百度/搜狗的索引早就收录了那些页面，点开就等于在平台内搜索，
> 绕开登录、永不 404。真正能自动抓的（实习僧、应届生求职网、政府公告、各校就业网）
> 已经全部接进采集器，跑 `抓取数据.bat` 就进「🔍 岗位列表」。

### 🧭 不用装扩展的替代路径：网页里的「📝 简历定制」页签

如果不想折腾油猴，也可以完全在 InternHub 网页里干：

1. 打开页面 → **「📝 简历定制」** 页签
2. 从投递箱带一条 / 手动填公司+岗位，把 JD 整段粘进去
3. 页面自动给出：**岗位方向识别**（可手动纠正）→ **匹配度分 + 命中的关键词 + JD 提到但你没体现的**（后者面试会被追问，别硬编）
4. 点「生成这份岗位的定向简历」→ 得到 docx / pdf，直接下载上传
5. 下面 20 项**网申填写文案**，每块右上角一键复制，粘进网申表单即可

同一页还能下载：投递工作台.html、网申助手.user.js、网申书签.txt、网申速填卡.txt、万能通用版简历。
成品材料都在 **`简历材料/`** 目录；内容源是 `data/resume_kit.json`，改完跑
`venv\Scripts\python.exe gen_resume_kit.py`（或双击 `生成简历材料.bat`）整套重生成。

### 📥 采集助手（把登录平台的岗位带回工具）
**现在右下角有 2 个按钮**：
- **📥 采集本页岗位** —— 采集并导出 CSV
- **🐞 结构诊断** —— 抓不到数据时点它，导出页面结构说明（发我即可精确定位问题）

**能采三种页面（会自动判断，不用你选）：**
| 页面类型 | 例子 | 说明 |
| --- | --- | --- |
| 岗位卡片列表 | 国聘 / 24365 / BOSS / 智联 / 牛客 / 实习僧 | 逐个卡片抓标题、公司、城市、链接 |
| 网页表格 | 高校就业网的"招聘岗位表"、Excel 转的网页 | **按表头自动对列**（岗位/单位/城市/学历/薪资/链接/截止…），没认出来的列塞进"描述"不丢信息 |
| 在线多维表格 | **飞书多维表格 / 钉钉表格**（共享链接打开的那种） | 先自动滚动（最多 9 秒）把行加载出来，再用**不依赖 class 名**的"重复行结构"识别整张表 |

1. 打开目标页面（**登录状态下**，筛选到你想要的范围）
2. 点 **📥 采集本页岗位** → 自动滚动 → 提示"识别方式：三维表格(选择器)/重复行结构(兜底)/网页表格(table)/岗位卡片，识别到 N 条" → 确认后下载 CSV
3. 回到网站 →「📚 备考方案」拉到最下面 → **上传该 CSV**，或用 **📋 直接粘贴导入**
4. 之后就能在「岗位列表」里统一筛选、加投递箱、生成定制简历

> ⚠️ **几千行的在线表格不要指望全抓**：网页只渲染屏幕附近几十行。**先在飞书里筛选/搜索**（例如只要云南、2027届），把结果压到几百行以内再采集。
> 如果诊断文件里写着 `canvas 数量 > 0` 且没有候选行 → 那张表是用 canvas 画的，脚本读不到，请用「📋 直接粘贴导入」。

### 🔗 采集书签（装不上扩展时的替代方案）
见 `采集书签说明.md`。功能与采集助手**完全一样**（同一份源码生成），只是做成 `javascript:` 书签，免装油猴。

### 🔖 网申书签（**装不上油猴也能自动填表**）
同目录文件 **`网申书签.txt`** —— 和采集书签一样的用法：浏览器书签栏 → 右键新建书签 → 网址粘贴这一整行 `javascript:...`。
打开网申页 → 点这个书签 → 按「只填空字段」填一遍，并弹窗告诉你填了哪些字段。

| 能力 | 网申书签 | 网申助手（油猴版） |
| --- | --- | --- |
| 文本框 / 长文本 / 下拉 / 单选 / 日期 | ✅ | ✅ |
| **iframe 里的表单** | ❌ 书签进不了子框架 | ✅（postMessage 广播） |
| 资料面板（当场补改字段） | ❌ | ✅ |
| 只填空 / 覆盖全部 两种模式 | 只有"只填空" | 都有 |
| 装扩展 | 不用 | 要油猴 |

> 结论：**能装油猴就用油猴版**（更强）；装不上就用书签版救急，表单在 iframe 里的少数系统手工填。

### 🔄 改了资料/脚本后要重新生成
采集助手与采集书签都从源码生成，改完代码后跑一次即可同步：

```bat
venv\Scripts\python.exe -m ihub.collector
```

它会同时刷新 `采集助手.user.js` 和 `采集书签.txt`（并校验书签版语法，避免压行压坏）。

## 看不到按钮？按这个顺序查（30 秒）

**最省事的确认办法**：装好脚本后**刷新 InternHub 网站**（http://localhost:8501）——如果右下角多出一个蓝色
「📝 网申助手」胶囊，说明脚本已经在运行了；**任何网站**（含 zhaopin.cnpc.com.cn 这类国企平台）都该出现它。

| 顺序 | 检查项 | 怎么做 |
| --- | --- | --- |
| 1 | **油猴装了吗** | 浏览器右上角有没有**黑色 Tampermonkey 图标**（拼图 → 显示在工具栏）。没有 → Edge「扩展」→ 获取 Microsoft Edge 扩展 → 搜 `Tampermonkey` |
| 2 | **脚本装了吗** | 点油猴图标 → **管理面板** → 有没有 `网申助手 v2` / `采集助手`，状态是否「已启用」 |
| 3 | **真的点"安装"了吗** | 把 `网申助手.user.js` 拖进浏览器窗口 → 会打开一个**安装页** → 必须点蓝色的「**安装**」。只在页面里看到一堆代码 = 还没装 |
| 4 | **是不是旧版本** | v1 用的是 `@grant none`，会被国企/政府网站的**内容安全策略(CSP)** 直接拦掉（症状就是"页面上什么都没有"）。v2.1 起已改成 GM 沙箱模式，重新安装即可 |
| 5 | **本地文件不生效是正常的** | 浏览器默认不允许脚本在 `file:///` 页面上运行。要测本地页面，需在油猴「设置 → 配置模式=高级 → 安全 → 允许脚本访问文件网址」勾选（Chrome/Edge 还要在扩展详情里打开「允许访问文件网址」） |
| 6 | **特殊页面** | `edge://`、`chrome://`、扩展商店页、PDF 阅读器等浏览器内置页永远不会有按钮 |

**还有一张「假网申表」可以练手**：双击桌面 `简历\脚本自检.html` —— 里面有一张仿国企网申的表单（姓名/性别单选/出生日期/身份证/手机/邮箱/学校/学院/专业/学历下拉/籍贯下拉/自我评价），
点右下角 📝 → 「填入空字段」，就能看到真实效果。页面上还会显示两个脚本的检测结果（本地文件需要第 5 步的设置才会显示 ✅）。

## 常见问题
| 现象 | 解决 |
| --- | --- |
| 右下角没有按钮 | 按上面 6 步排查；确认脚本**已启用**、刷新页面（SPA 页面等 2 秒，脚本会自己重挂按钮） |
| 拖拽 .user.js 没反应 | 打开"开发者模式"，或改用**粘贴安装**（油猴 → 添加新脚本 → 粘贴 → Ctrl+S） |
| 网申助手提示"已填 0 个字段" | 把该页网址发我加规则；或先在页面里滚一下让表单完全渲染出来再点；iframe 表单等 1～2 秒 |
| 填错了/填串了 | 用「覆盖全部」重填；或把资料里那个字段清空（留空=跳过）；面板里改的值标 `*`，点「恢复内置」可清掉 |
| 表单在 iframe 里没反应 | 正常应该能填；若该 iframe 是 `sandbox` 隔离的，浏览器不允许任何脚本进去，只能手动填 |
| 采集到的岗位偏少/偏多 | 先把列表**滚动加载完**再点；或先筛选城市/关键词再采集 |
| 点了没反应（书签版） | 该站点 CSP 拦截了书签脚本，改用油猴版或"方案 B：另存网页" |
| 想卸载 | 油猴管理面板里删除对应脚本即可 |

## 参考的同类项目（我做 v2 时对着它们的功能清单设计的）
| 项目 | 特点 | 和我们的区别 |
| --- | --- | --- |
| [OpenJobAutofill](https://github.com/Br1an67/OpenJobAutofill) | 开源、隐私优先，带 AI 页面分析 | 我们是纯规则匹配、**不联网**，不需要 API Key；它分析能力更强但要配置模型 |
| [Simplify Copilot](https://simplify.jobs/copilot) | 国外最流行的网申自动填 + 投递跟踪 | 面向欧美 ATS 系统，字段体系跟国内的"政治面貌/籍贯/生源地"对不上 |
| [OfferNow 简历闪填](https://github.com/ruanyf/weekly/issues/8577) | Chrome 插件，AI 语义识别，主打国内招聘网站 | 需要装真正的扩展 + 走它的服务；我们的脚本把资料留在本机 `profile.json` |
| [简历自动填写助手](https://chromewebstore.google.com/detail/%E7%AE%80%E5%8E%86%E8%87%AA%E5%8A%A8%E5%A1%AB%E5%86%99%E5%8A%A9%E6%89%8B/pplphmnbdjnholichjplcgakhlmanakp) | 商店里的填表助手 | 同上；且第三方插件会把你的简历信息上传到它的服务器 |
| [Simplify 的填表原理说明](https://simplify.jobs/copilot) | 说明"字段识别 + 本地资料库"这一套思路 | 我们做的就是这套的精简版：字段字典 + 本机资料 |

**一句话选择**：想要 AI 猜字段、愿意把简历交给第三方服务 → 用商店里的插件；想**资料完全不出本机、不要 API Key、字段按国内网申定制** → 用我们的 `网申助手.user.js`。

## 脚本坏了怎么自测（我改代码时用）
```bat
venv\Scripts\python.exe tests\run_all.py
```
会依次校验：脚本生成 → **网申助手 55 项**（现代表单/表格表单/下拉/单选/日期/富文本/iframe/只填空/覆盖/防乱填/面板）→ 采集助手 25 项 → 采集书签 11 项 → CSV 导入 34 项 → 网页冒烟 16 项。
会依次校验：采集脚本生成 → 油猴版解析表格/卡片 → 书签版解析 → CSV 导入入库（共 60+ 项断言）。

---
*以上脚本仅在本机生成、仅你本人手动触发使用；请勿用于批量刷投或采集他人隐私数据。*
