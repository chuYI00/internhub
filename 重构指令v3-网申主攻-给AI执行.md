# InternHub 重构指令 v3 —— 网申主攻版（直接复制给 AI 执行）

> 项目：`D:\罗广睿\intern_tool`。本指令在 v2 基础上调整和新增，**与 v2 冲突处以本指令为准**。本轮五个任务按顺序做，每步跑 `tests/run_all.py` 全绿再继续。

---

## 任务一（最高优先）：网申助手 v5 —— 这轮的命根子，全力做到最好用

用户原话："投最关键的就是网申助手，一定要好用、能用、真帮我填，不用我自己一个一个动手。"

### 必读参考项目（真实开源，去抄作业）

| 项目 | 抄什么 |
|---|---|
| `friuns2/cdp-autofill`（GitHub） | **三级 label 识别**：`aria-labelledby` → `label[for]` → `placeholder` → `name/id` 反推字段语义；AI 兜底匹配思路 |
| `athreygonella/QuickApply`（GitHub，Workday 专用） | **profile.json 数据结构**（个人信息/教育起止年/是否愿意搬迁等标准问答字段）；text/dropdown/radio 三类控件的处理写法；加载后视觉反馈按钮 |
| `mixterjim/AutoFill`（greasyfork #472933） | **Shadow DOM 悬浮面板**（防被网站样式污染遮挡）；GM_setValue 本地存储；设置弹窗交互 |
| 浏览器扩展 AutoFill Forms（Chrome 商店，开源） | 多档 Profile + 正则规则匹配 |

### v5 功能蓝图（在现有 v4 上升级）

1. **字段识别引擎**：label 匹配升级为三级（精确 → 模糊 → 上下文推断：兄弟节点/前驱文本）；字段库 40 → **60+ 类**，重点补**国企网申特色字段**：紧急联系人及关系、期望薪资、到岗时间、政治面貌、民族、生源地/高考省份、身高体重、奖惩情况、家属信息、学籍学历起止年月、证书等级（四六级/计算机）、是否服从调剂等。
2. **多档数据**：数据结构支持 `字段 → {default, by_role: {岗位方向: 值}}`——"期望薪资""自我评价"等技术岗/操作岗填不同值（现有 9 套方向话术沿用）。
3. **填充报告**：一键填充后不静默——出报告：✅ 成功 N 项（列表）/ ⚠️ 需人工 M 项，每项可**一键滚动定位到该字段**并标红描边；每字段保留「📋」复制兜底。
4. **字段学习机制（关键）**：遇到没识别的新字段，用户手动填一次 → 点面板上的"🧠 记住这个字段"→ 把"label 特征 → 数据字段"映射存入本地 JSON → 下次任何网站自动认得。**这是把"不好用"变成"越用越好用"的核心，必须做。**
5. **触发方式**：`@match *://*/*` 但不自动跑——页面右下角悬浮球，点击展开面板（选方向 → 一键填充 → 报告）。Shadow DOM 隔离样式。
6. **测试**：用 Playwright 搭本地测试页集（模拟 北森 / Moka / 自研国企网申 三种典型结构，含自绘下拉、卡片单选、iframe、分步表单），现有 `autofill_dom_test.js`(55)+`autofill_ats_test.js`(24) 扩充到覆盖新字段和学习机制。

### 交付物
- `网申助手.user.js` v5 + `网申书签.txt`（书签版保留兜底）+ 使用说明（3 步上手，带截图位）。

---

## 任务二：岗位页排序与筛选

岗位列表顶部加工具条：
- **排序**：发布时间（新→旧）/ 截止日期（近→远）/ 公司名。
- **筛选**：城市多选（默认昆明+大理）、关键词搜索、时间范围（近 7 / 14 / 30 天 / 全部）、"报名中"开关（已截止的默认折叠）。
- 每张岗位卡显示：发布日期 + 截止日期 + 剩余天数（临期 3 天内标红）。
- 后端：岗位入库时必须有 `发布时间` 字段，采集和导入缺失时记 `来源抓取日`，不许出现空值排序错乱。

## 任务三：界面新手友好（延续 v2 的主题 CSS / 导航条 / 卡片，追加）

- 首次打开显示**新手引导条**：4 步卡片（找岗位→投递→简历→备考），每步一句话"点什么、得到什么"，可关闭（localStorage 记忆）。
- 每个页签顶部一句"这一页帮你做什么"+ 主按钮高亮。做到**不是作者的人 3 屏内知道下一步点哪**。
- 侧边栏维持 v2 的 4 项 + 维护 expander。

## 任务四：文件清理 —— 只列清单，不许直接删

⚠️ 严格规则：**本轮你只生成 `待清理清单.md`，不执行任何删除。**

1. 全项目扫描（**不含 venv/**），列出候选：`backup/*.bak`、已完成使命的旧指令文档（重构指令-给AI执行.md、重构指令v2）、`解码飞书表格.py` + `飞书只读表格导出实操指南.md`（用户已放弃飞书导入）、重复/过期的说明 md、`data/` 里无代码引用的 json。
2. 清单每条写：`路径 | 是什么 | 为什么候选 | 最后被谁引用（grep 结果）`。
3. **用户回复确认后**才执行删除；删除前必须 `git commit` 一次当前状态（保证可回滚）。清单之外的任何文件一律不碰。

## 任务五：git 提交推送

1. 先检查 `.gitignore`：`data/resume_kit.json` 等含**手机号/邮箱/学号的个人数据文件**必须被排除；若历史上已提交，用 `git rm --cached` 移除追踪（本地文件保留）并向用户说明。
2. 本轮全部改动整理成 1~2 个语义化 commit（如 `feat: autofill v5 + 岗位排序筛选`），推送到 GitHub `chuYI00/internhub`。
3. 推送前 `git status` 确认无意外文件混入。

---

## 验收标准

1. 网申助手 v5：在本地 Playwright 三种模拟网申页上，60+ 字段类型填充成功率报告 + 学习机制演示（填一次→记住→第二次自动识别）。
2. 岗位页：按发布时间/截止日期排序正确，筛选组合不冲突，临期标红。
3. 新手引导条显示/关闭正常。
4. `待清理清单.md` 存在且未执行删除。
5. `tests/run_all.py` 全绿；git 已推送且个人敏感文件未被追踪。

## 执行顺序

**任务一（网申）→ 任务二（排序筛选）→ 任务三（引导）→ 任务四（清单）→ 任务五（git）**。任务一做完先交付让用户实测，别憋到最后。
