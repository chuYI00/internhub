javascript:(()=>{const K=/招聘|校招|实习|岗位|工程师|专员|经理|助理|顾问|开发|测试|运营|销售|管培/,S=/登录|注册|首页|更多|下一页|上一页|隐私|协议/;const clean=s=>(s||'').replace(/\s+/g,' ').trim();const city=t=>{for(const c of ['潍坊','济南','青岛','烟台','昆明','大理','曲靖','玉溪','北京','上海','深圳','广州','成都','杭州','南京','武汉','西安','重庆','天津','长沙'])if((t||'').includes(c))return c;return ''};const rows=[],seen=new Set();const push=el=>{const a=el.querySelector&&el.querySelector('a[href]');if(!a)return;const title=clean(a.getAttribute('title')||a.innerText||'');const href=a.href||'';if(!title||title.length<6||S.test(title)||!K.test(title))return;if(!/^https?:/.test(href)||seen.has(href))return;const lines=(el.innerText||'').split('\n').map(clean).filter(Boolean);let comp='';for(const ln of lines){if(ln!==title&&ln.length>=4&&ln.length<=30){comp=ln;break}}seen.add(href);rows.push([title,comp,city(el.innerText)||city(title),href,clean(el.innerText).slice(0,200)])};document.querySelectorAll('li,.job,.job-item,.position,.list-item,tr,.card,article').forEach(push);if(rows.length<5){document.querySelectorAll('a[href]').forEach(a=>push({querySelector:()=>a,innerText:a.innerText||''}))}if(!rows.length){alert('没识别到岗位：请先把岗位列表滚动加载完，再点一次');return}if(!confirm('识别到 '+rows.length+' 条岗位，导出 CSV？'))return;const host=location.hostname.replace(/^www\./,'');const head=['岗位','公司','城市','学历','标签','链接','截止日期','描述','岗位类型','届别','来源'];const q=v=>'"'+String(v||'').replace(/"/g,'""')+'"';const body=rows.map(r=>[q(r[0]),q(r[1]),q(r[2]),'',q(host),q(r[3]),'',q(r[4]),'秋招','2027届',q(host)].join(','));const blob=new Blob(['\ufeff'+head.join(',')+'\r\n'+body.join('\r\n')],{type:'text/csv;charset=utf-8'});const dl=document.createElement('a');dl.href=URL.createObjectURL(blob);dl.download='岗位采集_'+host+'_'+new Date().toISOString().slice(0,10)+'.csv';dl.click()})()
