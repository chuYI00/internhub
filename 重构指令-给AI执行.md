# InternHub 重构指令（直接复制给 AI 执行）

> ## ✅ 执行状态：4 步全部完成（2026-09-16）
>
> | 步骤 | 内容 | 状态 | 落地位置 |
> |---|---|---|---|
> | 1 | 收敛页签 12 → 4 | ✅ 已提交推送 | `app.py`（4 页签，编号与 `with tabN:` 一一对齐，冒烟测试有静态防线） |
> | 2 | 链接体检、砍伪直达 | ✅ 已提交推送 | `ihub/linkcheck.py` + `run_linkcheck.py` + `链接体检报告.md`（A 30 / P 25 / B 1 / C 0 / D 49） |
> | 3 | 台账打通网申助手 | ✅ 已提交推送 | `ihub/ledger.py`（烟草 1 单位 1 岗拦截）+ `autofill.py` 面板投递登记 + 岗位页 ↔ 简历定制联动 |
> | 4 | 备考方案生成器 | ✅ 已完成 | `ihub/studyplan.py` + `run_studyplan.py` + `生成备考方案.bat` + `备考冲刺资料/备考方案_*.md/docx` |
>
> 验收：`tests/run_all.py` **14 组全绿**（含页签对齐、链接域名与公司名匹配、烟草拦截、备考方案完整性）。
>
> 背景：项目是 `D:\罗广睿\intern_tool`（Streamlit + SQLite），现在 12 个页签、功能有重叠、链接质量参差，作者自己都不知道从哪用起。这次不做新功能堆砌，做**收敛重构**。

---

## 一句话目标

把 InternHub 从「12 个页签什么都有一点」重构成一条主线的秋招工作台：

**看岗位 → 官方投递 → 网申填表 → 定制简历 → 备考目标单位**

---

## 现状问题（必须解决）

1. 页签太多（12 个），功能重叠，没有清晰的使用主线。
2. 部分渠道链接点开后**不是对应企业的页面**。
3. 通过百度 `site:` 搜索直达生成的链接，实际**只能打开百度页**，不是目标页面——这类"伪直达"全部要去掉。
4. 实习信息和秋招信息混在一起，没有主次。

## 新定位（用户目标已变更）

- 主战场：**秋招**，地点**昆明 + 大理**，央国企为主、不限企业，岗位偏好技术类（不要一线操作岗）。
- 实习功能**保留但降级**：不删代码，在岗位页做成一个「秋招 / 实习」切换开关，默认秋招。
- 重点监控：**云南烟草**公告发布（国家烟草专卖局「人才招聘专栏」），监控已在后台自动化跑，保持不动。
- 新增通用能力：**备考方案生成器**——任何用户选一个目标单位/岗位，就生成一份像教培老师那样专业的备考方案（不写死只服务烟草）。

---

## 新信息架构：12 页签 → 4 页签

| 页签 | 内容 | 合并了原来的 |
|---|---|---|
| **1. 🎯 岗位** | 秋招岗位流（默认昆明/大理筛选，可切「实习」开关）：岗位名、公司、地点、报名窗口、**官方投递直达按钮** | weblist 展示、yunnan 渠道地图、platforms 平台矩阵（收进"更多入口"折叠区） |
| **2. 📮 投递与网申** | 三块：① 投递台账（投了什么、什么状态，防重复投）；② 渠道清单下载（txt/CSV）；③ 网申助手 user.js + 书签的下载和使用说明 | linklist、channels 说明、autofill 说明页 |
| **3. 📄 简历定制** | 选岗位方向 → 匹配分析 → 生成定向简历（docx/pdf）+ 网申文案 | tailor |
| **4. 📚 备考方案** | 通用备考方案生成器（见下） | study 框架 |

- 页签编号必须与 `with tabN:` 一一对齐（历史上出过渲染错位的坑，冒烟测试查不出来）。
- 烟草监控不占页签，新增公告在「🎯 岗位」顶部以横幅提示。

---

## 各模块具体要求

### 1️⃣ 岗位获取（Tab 1）
- 默认视图 = **秋招 × 昆明/大理**，每条岗位必须给「官方投递直达」按钮。
- 顶部横幅：烟草监控的最新公告（报名窗口、剩余天数、是否限报 1 岗）。
- 「秋招/实习」为一级开关，实习数据保留，切过去就能看。

### 2️⃣ 链接质量（硬标准，全项目适用）
- **只保留 A 级渠道**：企业官网招聘频道或官方公告原文里解出的网申地址。
- 每条链接必须满足：① 真实可达（HTTP 200）；② 打开后**页面内容确实属于该企业**（标题/域名能对上）；③ 是投递入口本身，不是搜索页、不是聚合平台转发页。
- 达不到标准的链接一律降级：不作为主按钮，收进"其他来源"折叠，且明确标注来源平台名。
- 百度 `site:` 搜索直达这种"只能打开百度"的伪直达全部移除。
- 逐条复查 `ihub/channels.py` 的 50 条映射，不合格的标记并修复或降级；`official_for()` 最长键匹配逻辑保留。

### 3️⃣ 网申助手（Tab 2，现 autofill v4）
- 能力不变：按岗位方向侧重填写、自绘下拉/卡片单选 ATS 兼容、📋 复制兜底。
- 补一件事：**和投递台账打通**——每家公司填完/投完能在台账里标记状态（待投/已投/笔试/挂了），防止"投了不知道投过"；烟草同批次只允许登记 1 单位 1 岗，重复登记给出强警告。

### 4️⃣ 简历定制（Tab 3，现 tailor）
- 逻辑不变：方向识别 → 匹配分析 → 单页定向简历（docx/pdf）+ 网申文案。
- 唯一改动：入口和岗位页联动——在岗位列表点某条岗位，能直接带岗位名跳到简历定制页。

### 5️⃣ 备考方案生成器（Tab 4，新能力）
做成"像教培老师一样"，两步：
- **选目标**：目标单位（如 云南中烟 / 云南省烟草专卖局 / 国家电网云南公司 / 自定义）+ 目标岗位类型 + 考试日期 + 每天可学时长。
- **生成方案**，包含：
  1. 考情分析：考试科目/题型/分值结构（按该单位公开大纲或历年惯例）
  2. 重点排序：该抓什么、可以放弃什么（按提分性价比）
  3. 分阶段计划：以考试日倒推，按剩余天数排出阶段任务和每日时间表
  4. 必背清单：高频考点、行测速记、专业知识要点、时政范围
  5. 真题与资料来源：官方渠道优先，注明获取方式
- 内置一份高质量的**云南烟草冲刺模板**（当前 9 月、考试临近的场景）：行测提速策略、烟草行业常识必背清单（国家局架构、云南烟叶地位、"两烟"大省数据、专卖法要点）、专业知识侧重（电气/机械/计算机按岗位分）、每日 6 小时冲刺课表。
- 方案可导出 md/docx。

---

## 验收标准（做完必须自检）

1. 页签恰好 4 个，`st.tabs` 编号与 `with tabN:` 一一对齐。
2. 每条渠道链接人工抽查 ≥10 条：浏览器打开后确实是该企业官方投递页，不是百度、不是聚合平台。
3. 「秋招/实习」开关正常，实习历史数据不丢。
4. 投递台账可新增/改状态/去重提醒。
5. 备考方案生成器：选"云南烟草"能出完整冲刺方案（含必背清单和课表），选其他单位也能出合理方案。
6. `tests/run_all.py` 全绿；冒烟测试增加：页签对齐检查、链接域名与公司名匹配检查。
7. `app.py` 顶部加一个 3 行以内的"使用主线"说明（看岗位 → 投递 → 网申 → 备考）。

## 执行方式（分 4 步提交，不要一次性大改）

1. ✅ **收敛页签**：12 → 4，只做搬移合并，不改逻辑。
2. ✅ **链接体检**：逐条验证渠道链接，砍伪直达，更新 `channels.py`。
3. ✅ **台账打通**：投递台账 + 网申助手/岗位页联动。
4. ✅ **备考生成器**：先做框架和云南烟草冲刺模板，再泛化成通用。

每步完成后跑 `tests/run_all.py`，报结果再做下一步。
