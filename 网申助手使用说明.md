# 网申助手 v5 · 3 步上手

> 一句话：**打开网申页 → 点悬浮球 → 点「填入空字段」**，剩下的看报告。
> 认不出的字段教它一次，以后所有网站都认得。

---

## 第 1 步：装（只做一次，2 分钟）

### 方式 A：油猴插件（推荐，最省事）
1. Chrome / Edge 装 **Tampermonkey**（扩展商店搜即可）。
2. 打开 `网申助手.user.js`，点「安装/重新安装」。
3. 装完右上角看到 Tampermonkey 图标里有 `网申助手 v5` 即成功。

> 已声明 `@grant GM_getValue/GM_setValue`，**政府网、国企站（有 CSP 限制）也能跑**。

### 方式 B：书签（不想装插件时用）
1. 打开 `网申书签.txt`，把里面那一长串 `javascript:` 整行复制。
2. 浏览器收藏夹 → 添加书签 → 名称写「网申助手」→ 网址粘贴进去。
3. 用的时候打开网申页，点一下这个书签。

⚠️ 书签版在少数有严格 CSP 的政府网站上会被拦，那时换方式 A。

---

## 第 2 步：补一次资料（一次性，5 分钟，之后终身 100%）

点右侧悬浮球 → 展开 **「📇 我的资料」**，打开 **「只看缺的」** 开关 —— 只列还没填的字段。

我现在替你填好的（已核对你的公开信息）：

| 字段 | 值 |
|---|---|
| QQ | 2810845\*\*\*（从 QQ 邮箱推出来的，本机 `data/profile.json` 里是全的） |
| 通讯地址 / 邮编 | 云南省大理白族自治州 / 671000 |
| 高考省份 | 云南 |
| 学位 / 学制 / 培养方式 | 学士 / 四年 / 统招 |
| 专业类别 | 工学（计算机类） |
| 英语四级成绩 | 446 |
| 社会实践 | 政府助理 + 家教（从经历里摘的） |
| 奖惩情况 | 无处分 + 三年奖学金 |
| 亲属在本单位 / 服从调剂 | 否 / 是 |
| 档案所在地 / 信息来源 | 中国民航大学 / 学校就业网·企业官网 |
| 期望城市 / 兴趣爱好 | 昆明、大理 / 编程开源、短视频、跑步 |

**这些必须你自己填**（别人替不了，填一次永久生效）：

| 字段 | 说明 |
|---|---|
| 身份证号 | 很多国企必填 |
| 微信号 | |
| 身高 / 体重 | 部分国企/机场岗要 |
| 户口性质 | 农业 / 非农业（看户口本首页） |
| 计算机等级 | 没考就填「无」 |
| 普通话水平 | 没考就填「无」 |
| 专业排名 | 如「前 30%」，不知道就问辅导员 |
| 入团时间 | 不记得就填「2019.05」或问团支书 |
| 紧急联系人 姓名 / 电话 / 与本人关系 | 填父母 |
| 家庭主要成员 | 如「父亲 罗XX 务农 / 母亲 XX 务农」 |
| 银行卡号 | 部分单位入职才要，暂时可空 |

> 面板里每一项都能直接改，改完**自动存本机**（油猴存储 / localStorage），
> 关掉浏览器也在。**不会写回 `data/profile.json`**，两边互不干扰。

---

## 第 3 步：用（每份网申 30 秒）

1. 打开任意网申页（北森 / Moka / 智联 / 企业自研都行）。
2. 右下角有个 **🤖 悬浮球**，点开面板。
3. 选**岗位方向**（技术通用 / 嵌入式硬件 / 物联网 / AI / 自动化 / 测试 / 民航 / 生产 / 新媒体）。
   - 想让「期望薪资、求职意向、到岗时间」按这个方向重写 → 点 **⤵ 用本方向值**。
   - 不想覆盖已填的，就别点，只影响空缺字段。
4. 点 **🚀 填入空字段**。
5. 看**填充报告**：
   - ✅ **已填 N 项**（可折叠，点开看明细）
   - ⚠️ **需人工 N 项** —— 每行点一下就**自动滚过去并红框高亮**，右边 **📋** 一键复制值，手动粘贴即可。
     - 最常见的两种情况：① 资料里这项是空的 ② 这框是只读的（只能复制粘贴）

### 分步表单（Moka 那种一步一屏的）
**每翻一步就点一次「填入空字段」**，只填当前屏可见的字段。

---

## 核心新功能：教一次，终身认识

遇到它认不出的字段（比如「生源地(高考时户籍)」「有无直系亲属在本公司任职」）：

1. 点报告里的 **「这是什么？」** 下拉，选对应字段（比如 `hometown 籍贯`）。
2. 点 **记住**。
3. 完事。**以后任何网站**只要出现这个叫法（或相似叫法），它都自动填。

- 面板里有 **导出 / 导入** 学习库 —— 换电脑时导出一个 JSON，新机器导入即可。
- 认错了？在学习区找到那条，点 **删除** 就忘掉。

**匹配优先级**：你教过的 > 精确名 > 模糊关键词 > 上下文推断（fieldset 标题 / 表格行首列）。

---

## 这次实测（真 Chrome 跑的三种网申结构）

| 页面结构 | 字段数 | 自动填充 | **识别率** | 需人工 |
|---|---|---|---|---|
| 北森式（antd 自绘下拉 + 卡片单选） | 58 | 44 (76%) | **100%** | 13 |
| Moka 式（3 步分步表单） | 62 | 45 (73%) | **100%** | 6 |
| 国企自研（老表格 + 只读框 + 富文本） | 58 | 44 (76%) | **100%** | 13 |

**识别率 100%** = 没有一个字段是"看不懂"的。
填不满的 100% 是资料空缺 —— 按第 2 步补齐后，这三页都能到 **95%+**（剩下的只是只读框和富文本，靠 📋 复制）。

自检页在 `tests/fixtures/` 下三个 html，双击就能在浏览器里自己试。

---

## 常见问题

**Q：点了没反应？**
A：先看悬浮球在不在右下角。不在 → 刷新页面；在但点开空白 → 换方式 A（油猴版）。

**Q：会不会把已有内容改掉？**
A：不会。**只填空的**，有值的框一律跳过（除非你明确点了「⤵ 用本方向值」）。

**Q：下拉框没选上？**
A：自绘下拉（antd/element）要"点开→点选项"，脚本已处理；但有些站点点开有动画延迟，
   多等半秒再点一次「填入空字段」即可。报告里会列为"需人工"。

**Q：隐私安全吗？**
A：数据全在本机（油猴存储 / localStorage），脚本**不联网、不上传**任何东西。

---

## 文件对照

| 文件 | 用途 |
|---|---|
| `网申助手.user.js` | 油猴脚本（方式 A） |
| `网申书签.txt` | 书签版（方式 B） |
| `ihub/autofill.py` | 上面两个的**唯一数据源**，改资料后跑 `python -m ihub.autofill` 重新生成 |
| `tests/fixtures/网申测试页_*.html` | 三种结构的自检页，双击可玩 |
| `tests/autofill_browser_test.py` | 真实浏览器验收（跑填充率） |
