# 采集书签（无需安装任何扩展）

> 适用：你已经登录了国聘 / 24365 / BOSS / 智联 / 牛客，但**不想/装不上油猴扩展**时，用"书签脚本"一键导出岗位 CSV。

## 一、怎么用（1 分钟设置，永久可用）

1. 在浏览器里按 **Ctrl + Shift + B** 显示书签栏
2. 在书签栏空白处 **右键 → 添加网页/新建书签**
   - 名称填：`采集岗位`
   - 网址（URL）填：**把下面这个文件里的整行 `javascript:...` 全部内容粘贴进去**
3. 打开目标网站，筛好岗位列表（滚动加载完）
4. 点书签栏的 **采集岗位** → 提示识别到 N 条 → 确认后下载 CSV
5. 回到 InternHub →「📚 备考方案」页底部**上传该 CSV** → 一键入库

## 二、要粘贴的代码

见同目录文件 **`采集书签.txt`**（打开后全选复制那一整行即可；必须以 `javascript:` 开头）。

> 注意：部分网站有 CSP 限制（如个别银行站点）会拦截书签脚本；国聘、BOSS、智联、牛客、实习僧一般可用。
> 若被拦截，用下面的"方案 B"。

## 三、方案 B：保存网页 → 本地解析（100% 可用，零依赖）

1. 在浏览器打开岗位列表页（登录状态、滚动加载完）→ 按 **Ctrl + S** → 保存类型选 **"网页，仅 HTML"** → 存到 `D:\罗广睿\intern_tool\导入网页\`
2. 在本项目目录运行：

```bat
venv\Scripts\python.exe run_parse_html.py
```

3. 脚本会把 `导入网页\` 里所有 html 解析成岗位并入库（自动识别岗位名/链接/城市/公司）
4. 然后刷新网站 → 「岗位列表」里就能看到这些岗位

> 或者：把保存好的 html 直接发我，我帮你解析入库。
