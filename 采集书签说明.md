# 采集书签（无需安装任何扩展）

> 适用：你已经登录了国聘 / 24365 / BOSS / 智联 / 牛客 / 飞书共享表，但**不想或装不上油猴扩展**时，用"书签脚本"一键导出岗位 CSV。
> 功能与 `采集助手.user.js` **完全一致**（同一份源码生成，已用 60+ 项断言自测过）。

## 一、怎么用（1 分钟设置，永久可用）

1. 在浏览器里按 **Ctrl + Shift + B** 显示书签栏
2. 在书签栏空白处 **右键 → 添加网页/新建书签**
   - 名称填：`采集岗位`
   - 网址（URL）填：**把 `采集书签.txt` 里那一整行 `javascript:...` 全部内容粘贴进去**
3. 打开目标网站，筛好范围（登录状态）；**表格页会自动滚动加载剩余行**，不用手动滚
4. 点书签栏的 **采集岗位** → 提示"识别方式：网页表格/卡片，共 N 条" → 确认后下载 CSV
5. 回到 InternHub →「📚 备考方案」页底部**上传该 CSV** → 一键入库

## 二、要粘贴的代码

见同目录文件 **`采集书签.txt`**（打开后全选复制那一整行即可；必须以 `javascript:` 开头）。

> 注意：部分网站有 CSP 限制（如个别银行站点）会拦截书签脚本；国聘、BOSS、智联、牛客、实习僧、飞书多维表格一般可用。
> 若被拦截，用下面的"方案 B"。

## 三、能采哪三种页面（自动判断，不用你选）

| 页面类型 | 例子 | 抓到的字段 |
| --- | --- | --- |
| 岗位卡片列表 | 国聘 / 24365 / BOSS / 智联 / 牛客 / 实习僧 | 岗位名、公司、城市、链接、卡片文字摘要 |
| 网页表格 | 高校就业网的招聘岗位表、Excel 转网页 | 按表头自动对列：岗位/公司/城市/学历/**薪资**/链接/截止日期/类型/届别；没认出来的列进"描述" |
| 在线多维表格 | **飞书多维表格**、钉钉表格（共享链接打开的那种） | 同上（先自动滚动把行加载满） |

表头写变体也能认：`职位名称`、`单位名称`、`工作城市`、`薪资待遇`、`报名截止时间`、`投递链接` 等。

## 四、方案 B：保存网页 → 本地解析（CSP 拦截时的兜底）

1. 在浏览器打开岗位列表页（登录状态、滚动加载完）→ 按 **Ctrl + S** → 保存类型选 **"网页，仅 HTML"** → 存到 `D:\罗广睿\intern_tool\导入网页\`
2. 在本项目目录运行：

```bat
venv\Scripts\python.exe run_parse_html.py
```

3. 脚本会把 `导入网页\` 里所有 html 解析成岗位并入库（自动识别岗位名/链接/城市/公司）
4. 然后刷新网站 → 「岗位列表」里就能看到这些岗位

## 五、方案 C：直接下载 CSV（最省事，多数平台都支持）

| 平台 | 导出方法 |
| --- | --- |
| 飞书多维表格（共享链接） | 打开后右上角 `…` → **下载为 CSV / 导出为 Excel**（需要你有阅读权限）。导出的 CSV 直接上传到 InternHub 即可，表头变体会自动识别 |
| 各类网申系统 / 微信群文件 | 一般本身就是 Excel/CSV，直接上传 |
| BOSS / 智联 / 牛客 | 无官方导出 → 用书签（方案 A）或另存网页（方案 B） |

## 六、两个第三方来源的处理结论

| 你给的东西 | 结论 | 你要做什么 |
| --- | --- | --- |
| 飞书多维表格 `ycnynolbv7lx.feishu.cn/base/M7p6bYxFWa3GcXscQ1HcHaAWnod` | 服务端抓不到：该页返回的只是空壳 HTML，**数据行要登录后由浏览器 JS 拉取**，不绕过登录 | ①（推荐）用上面方案 C 导出 CSV 发我/直接上传；② 或登录后点"采集岗位"书签 |
| 夸克网盘 `pan.quark.cn/s/82351fe71014` | 网盘分享需要**登录态 + 一次性提取令牌**，脚本无法直接下载，也不能绕过 | 你在网盘里**下载到本机** → 把文件（Excel/CSV/Word 都行）放进 `D:\罗广睿\intern_tool\导入网页\` 或直接发我，我来解析入库 |

## 七、改了代码后同步书签

`采集书签.txt` 是由源码生成的（保证与油猴版一致）。改完代码后：

```bat
venv\Scripts\python.exe -m ihub.collector
```

会同时刷新 `采集助手.user.js` 与 `采集书签.txt`；生成时会自动校验书签版语法（防止"压成一行"把代码压坏）。
生成后请把新的 `采集书签.txt` 内容**重新粘贴覆盖**书签网址（因为内容变长了）。

自测：

```bat
venv\Scripts\python.exe tests\run_all.py
```
