# InternHub 重构指令 v2 —— 可用性 + 美观（直接复制给 AI 执行）

> 项目：`D:\罗广睿\intern_tool`（Streamlit + SQLite）。上一轮已按《重构指令-给AI执行.md》完成 4 页签收敛 / 链接体检 / 投递台账 / 备考方案生成器，`tests/run_all.py` 14 组全绿。**本轮不做新功能堆砌，只做三件事：把"埋起来的功能"挖到台面上、把界面整理干净、把网申路线加一条兜底。**

---

## 用户原话（理解意图用，不用回复）

- "看似功能很多，实则很乱……我自己设计的都不知道怎么用。"
- "你让我浏览器点一下链接归档，但我点都点不了，要自己复制了去搜。"（**此 bug 阿枢已修：链接体检区 A/D/C 三处已改成 `<a href>` 真链接，你不要再动这段，但全项目自查一遍，凡是把 URL 渲染成 `code 文本` 而不是链接的地方都改成可点链接。**）
- "我在淘宝买了别人整理的飞书岗位文档，只读、不能复制、不能分享，我想把里面的岗位导进来。"
- "网申助手填进去不管用……实在不行你给我做一份全能简历（电气/电子/物联网/AI 都能投），我拿去用别的网申插件。"
- "界面能好看尽量好看，先实用。"

---

## 三大任务

### 任务一（P0）：导入向导 —— 解决"飞书只读文档怎么进来"

现状：粘贴导入功能**其实已经存在**（岗位页 expander + 备考页上方各一处），但埋得深、入口重复，用户根本不知道有。把它升级成岗位页的**一级区块**，并支持四条通道：

| 通道 | 场景 | 实现 |
|---|---|---|
| ① 上传 CSV / Excel | 手里有现成文件 | `st.file_uploader`，pandas 读取 |
| ② 复制粘贴 | 飞书多维表格能框选 Ctrl+C | 保留现有 `import_csv_text`，搬进向导 |
| ③ 抓包解码 | 飞书**多维表格**被禁复制/导出/打印（本项目实际场景） | 用户按《飞书只读表格导出实操指南.md》用 F12 Network 拿到 `H4sI` 开头的 base64 → 项目根已有现成解码器 **`解码飞书表格.py`**（已写好并自测通过：自动识别 gzip+base64 / JSON，嵌套 `{type:'text'}` 字段展平，递归找 records，输出 `data/飞书岗位导出.csv`）→ 向导里直接上传该 CSV。你的工作：把这个脚本的用法嵌进向导第③通道的引导文案 + 加一个"浏览该 CSV"按钮 |
| ④ 截图 OCR（兜底，100% 可行） | 前三条全被拦 | RapidOCR 逐屏截图识别成表格；依赖太重就最后做，先留接口 |

**统一流程（关键）**：选择通道 → 系统解析出原始表格 → **预览识别结果** → **字段映射确认**（用户的列名千奇百怪："公司名称/单位/雇主"都要能映射到标准字段）→ 勾选"确认入库" → 写入岗位库并去重。

标准字段：`公司 / 岗位名称 / 城市 / 投递链接 / 来源 / 报名截止 / 备注`。映射不到的列并进"备注"。

**入口要求**：岗位页顶部横向一块「📥 导入我的岗位表」，副标题一句话写清用法：
> "飞书文档被禁复制？按《飞书只读表格导出实操指南.md》操作——浏览器扩展一键导出，或 F12 抓包后用项目里的 `解码飞书表格.py` 解成 CSV。"

把现有 3 处重复导入入口**收敛成这 1 处**。写一版 `导入教程.md`，配三张示意图位置（文字描述即可，不用真截图）。

### 任务二（P1）：界面收敛 + 美化

**侧边栏大扫除**——现在抓取、筛选、体检说明全堆在左边。改成只留四样：
1. 城市筛选（默认 昆明+大理）
2. 秋招 / 实习 开关
3. 烟草监控状态（最近一次检查时间 + 新公告数）
4. 底部一个 `⚙️ 维护` expander：手动抓取、链接体检重跑、数据导出，全收进来

**页面规范**：
- 顶部加**主线导航条**：`① 找岗位 → ② 投递网申 → ③ 定制简历 → ④ 备考` 四步指示器（纯 st.columns + HTML/CSS 实现，当前页签对应步骤高亮），替代现在的大段文字说明。
- 岗位列表改**卡片式**：左列信息（公司加粗、岗位、城市标签、截止日期），右列一个主按钮「去投递」；同类信息对齐、留白统一。
- 定义一套主题 CSS（`st.markdown` 注入一次）：统一主色（建议深蓝 #1E3A5F + 强调橙）、按钮圆角、卡片阴影、字体层级。全项目只允许用这套变量，不许出现第二套随手写的颜色。
- 每个 tab 顶部最多一行标题 + 一行说明，操作说明全部收进 `?` tooltip 或 expander。
- 参考对象：Streamlit 官方 Gallery 里布局干净的 demo（卡片栅格、留白、单一主色）。

### 任务三（P1）：网申兜底 —— 全能简历 + 第三方插件路线

网申助手 v4 保留但降级为进阶选项（不再当主推，文案里注明"兼容性因企业 ATS 而异"）。新增主推路线：

1. **生成一份「全能简历」**：基于 `ihub/tailor.py` 与 `data/resume_kit.json`，新增 `universal` 方向——
   - 技能区写成四向矩阵：电气（PLC/电机/继电保护基础）、电子（STM32/FreeRTOS/UART/PCB）、物联网（ROS/SLAM/传感器网络）、AI（Python/机器视觉/大模型应用）；
   - 项目经历（机场巡检机器人）写成一段底稿 + 按四向各出一版侧重措辞；
   - 不裁剪、不分页删内容，单页排满为准。
2. 导出 **docx + pdf**（沿用现有 tailor 导出逻辑）。
3. 简历定制页顶部给「🌟 全能简历」一键生成按钮，旁边一段说明：把这份 PDF 传到第三方网申插件（如"塔塔简历"类网申助手）作为底稿，由插件负责各网站自动填——本工具负责**生成底稿 + 台账记录**。
4. 投递台账加一个"来源"字段：`本工具 / 第三方插件 / 手动`，第三方插件投的也能记进台账防重复。

---

## 验收标准

1. 导入向导：用一段模拟的飞书复制文本 + 一个乱列名 CSV 走通"解析 → 映射 → 入库 → 去重"全流程；抓包解码通道用一份模拟的 `H4sI` 开头 gzip+base64 数据验证（可参照 `解码飞书表格.py` 文件头注释自测）。
2. 侧边栏只剩 4 项 + 维护 expander；主线导航条在 4 个页签下都显示且高亮正确。
3. 全项目 grep 不到"把 URL 渲染成 `` ` `` 代码文本"的残留；岗位卡、渠道、体检区所有 URL 都是可点链接。
4. 全能简历能生成且单页排下，四向内容齐全。
5. `tests/run_all.py` 全绿；冒烟测试新增：① 侧边栏组件数量上限检查；② 页面中 `<a href` 与 `link_button` 的存在性抽查（防止又退化成纯文本）。
6. 界面自查：整页不允许出现三种以上主色调；每个 tab 从上到下 3 屏内能看到"下一步该干什么"。

## 执行顺序（分 4 步提交，每步跑全测试再继续）

1. **导入向导**（P0，先做）——含入口收敛 + 导入教程。
2. **侧边栏收敛 + 重复入口合并**。
3. **主题 CSS + 主线导航条 + 岗位卡片**。
4. **全能简历 + 台账来源字段**。

做完第 1 步就先让用户试用导入，别四步攒一起交付。
